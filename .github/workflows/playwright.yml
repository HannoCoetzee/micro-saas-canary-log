name: Playwright E2E Tests

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  e2e:
    uses: HannoCoetzee/fleet-ci/.github/workflows/playwright-tests.yml@main
    with:
      repo: micro-saas-canary-log
      ref: ${{ github.ref }}
    secrets: inherit
